#ifndef LINE_FOLLOW_ROBOT_UTILS_H
#define LINE_FOLLOW_ROBOT_UTILS_H

#include <line_follow_robot_consts.h>

const char* convertDirectionToString(Direction v);

const char* convertSideToString(Side v);

#endif